#blue
services:
  backend:
    # 현재 디렉토리에서의 Dockerfile을 사용하여 Docker 이미지를 빌드
    build: .
    # 호스트의 8081 포트와 컨테이너의 8000 포트를 매핑
    ports:
      - "8081:8000"
    # 컨테이너의 이름
    container_name: spring-blue
    environment:
      - TZ=Asia/Seoul

networks:
  backend-network:
    driver: bridge
